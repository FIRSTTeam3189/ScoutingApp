<?xml version="1.0" encoding="utf-8" ?>
<ContentPage xmlns="http://xamarin.com/schemas/2014/forms"
             xmlns:x="http://schemas.microsoft.com/winfx/2009/xaml"
             xmlns:widget="clr-namespace:Android.Widget;assembly=Mono.Android"
             x:Class="ScoutingApp.UI.HomeMyTeamPage">
  <StackLayout x:Name="Layout">
    <StackLayout Orientation="Horizontal">
      <Label Text="3189"/>
      <Label Text="Circuit Breakers"/>
      </StackLayout>
    <Label Text ="Events"/>
    <ListView x:Name="EventsListView">
      <ListView.ItemTemplate>
        <DataTemplate>
          <TextCell Text="{Binding Name}" Detail="{Binding Location}"/>
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>
    <Label Text ="Ranks"/>
    <ListView x:Name="RankListView">
      <ListView.ItemTemplate>
        <DataTemplate>
          <TextCell Text="{Binding Name}" Detail="{Binding Ranking}"/>
        </DataTemplate>
      </ListView.ItemTemplate>
    </ListView>
    </StackLayout>
</ContentPage>